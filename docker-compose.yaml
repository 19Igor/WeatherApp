# first shot
# services:
#   front:
#     container_name: react-front
#     build: 
#       context: ./FrontEndSide
#       dockerfile: dockerfile
#     ports:
#       - "5173:80"
#     volumes:
#       - ./FrontEndSide:/app
#       - /app/node_modules
#     stdin_open: true
#     tty: true
#   nginx:
#     image: nginx:latest
#     ports:
#       - "80:80"
#     volumes:
#       - ./nginx.conf:/etc/nginx/nginx.conf

# second shot
version: "1.1"
services:
  api:
    image: back
    container_name: back
    build: 
      context: ./BackEndSide
      dockerfile: dockerfile
    ports:
      - '8080:8080'
    networks:
      - app-network
      
  # it works
  front:
    image: web
    container_name: web
    build: 
      context: ./FrontEndSide
      dockerfile: ./dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    stdin_open: true
    tty: true
    networks:
      - app-network
  
networks:
  app-network:
    driver: bridge